export function Age(name, age){
    return `${name}'s age is ${age}`;
}

export function City(name, city){
    return `${name}'s city is ${city}`;
}

export function Hobby(name, hobby){
    return `${name}'s hobby is ${hobby}`;
}
